# spice-sim — rank 4, seq 2048, 276 iters
model: models/qwen3.5-35b-a3b
fine_tune_type: lora
grad_checkpoint: true
save_every: 100
steps_per_report: 10
steps_per_eval: 100
val_batches: 25
train: true
seed: 42
clear_cache_threshold: 50000000000
lora_parameters:
  rank: 4
  alpha: 8
  dropout: 0.05
  scale: 2.0
  keys:
  - self_attn.q_proj
  - self_attn.k_proj
  - self_attn.v_proj
  - self_attn.o_proj
num_layers: 40
learning_rate: 2.0e-05
batch_size: 1
grad_accumulation_steps: 4
iters: 276
max_seq_length: 2048
